import React from "react";
import { shallow } from "enzyme";
import MarkButton from "../index";

describe("MarkButton", () => {
  it("should render successfully", () => {
  //   const component = shallow(<MarkButton />);
  //   expect(component).toMatchSnapshot();
  });
});
